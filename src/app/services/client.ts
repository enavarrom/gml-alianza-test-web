export interface Client {
    sharedKey: string;
    businessId: string;
    email: string;
    phone: string;
    createdDate: string;
  }
  
  export interface ClientRequestRecord { sharedKey: string; businessId: string; email: string; phone: string; } 